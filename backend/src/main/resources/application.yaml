spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://aws-1-eu-central-1.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.ngtwukpkejtvwvajuwsq
    password: V9!rKq#72Lm@xT4pZs6_Ne
    driver-class-name: org.postgresql.Driver
  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:1025}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:false}
          starttls:
            enable: ${MAIL_SMTP_STARTTLS:false}
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  reminders:
    enabled: ${REMINDERS_ENABLED:false}
    cron: ${REMINDERS_CRON:0 0 9 * * *}
    from: ${MAIL_USERNAME:integrabackend@gmail.com}
  jwt:
    secret: ${JWT_SECRET}
    expirationMinutes: ${JWT_EXPIRATION_MINUTES:60}

server:
  port: ${PORT:8080}
  servlet:
    context-path: /api
